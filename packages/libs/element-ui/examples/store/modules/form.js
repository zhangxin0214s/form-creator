import Cookies from 'js-cookie'

const state = {
    formConfig: {},
    widgetList: []
}

const mutations = {
  
}

const actions = {
  
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
