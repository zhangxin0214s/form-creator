{
  "name": "subject-editor",
  "private": true,
  "version": "0.0.1",
  "description": "this is a editor for subject",
  "type": "module",
  "author": "zhangxin0214@tal.com",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "editor:dev": "pnpm recursive run dev --filter=editor",
    "editor:build": "pnpm recursive run build --filter=editor",
    "docs:dev": "pnpm recursive run dev --filter=docs",
    "docs:build": "pnpm recursive run build --filter=docs"
  },
  "dependencies": {
    "@vue/reactivity": "3.2.37",
    "esbuild": "0.14.54 -w\npnpm install esbuild@0.14.54",
    "pinia": "^2.0.17",
    "sortablejs": "1.14.0",
    "vitepress": "1.0.0-alpha.4",
    "vue": "^3.2.37"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "2.0.0",
    "vite": "2.7.3"
  }
}
